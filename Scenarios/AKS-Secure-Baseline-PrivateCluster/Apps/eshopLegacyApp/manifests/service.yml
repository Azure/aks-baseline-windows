apiVersion: v1
kind: Service
metadata:
    name: "eshop-win-mvc"
    labels:
        app: "eshop-win-mvc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "eshop-win-mvc"